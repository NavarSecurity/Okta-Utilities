{
  "name": "okta-policy-exporter",
  "version": "1.0.0",
  "description": "Read-only Okta policy and policy rule exporter for backup, review, migration, and drift detection inputs.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js --config config.json",
    "dry-run": "node src/index.js --config config.json --dry-run",
    "test": "node --test tests/*.test.js"
  },
  "keywords": [
    "okta",
    "iam",
    "policy",
    "export",
    "mfa",
    "password-policy",
    "app-sign-on"
  ],
  "author": "Navar IAM Utility Pack",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
